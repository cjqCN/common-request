
spring:
  application:
    name: common-request-test
  profiles:
    active: 'h2'

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 输出mybatis日志
    map-underscore-to-camel-case: true
  mapperLocations: classpath:mapper/*.xml
  typeAliasesPackage: demo.bean